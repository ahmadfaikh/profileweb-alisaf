import React from 'react';
import { ACHIEVEMENTS } from '../data/pesantrenData';

export const PrestasiGaleri: React.FC = () => {
  if (ACHIEVEMENTS.length === 0) {
    return null;
  }
  return null;
};
